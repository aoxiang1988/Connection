<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" >

    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />

    <!-- 定义自定义权限 -->
    <permission android:name="com.dten.myservicedemo.MY_SERVICE_PERMISSION"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:appCategory="audio"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAutoApplication" >
        <activity
            android:name="com.dten.myservicedemo.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.dten.myservicedemo.MyService"
            android:exported="true"
            android:permission="com.dten.myservicedemo.MY_SERVICE_PERMISSION" >
            <intent-filter>
                <action android:name="com.dten.myservicedemo.IMyService"/>
            </intent-filter>
        </service>
    </application>

</manifest>